<%= form_for(@wod) do |f| %>
  <% if @wod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wod.errors.count, "error") %> prohibited this wod from being saved:</h2>

      <ul>
      <% @wod.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<!--
  <select>
 <option><%= @movements.first %></option>  
 </select>
<select name="city_id" id="city_id">
  <option value="1">Lisbon</option>
  <option value="2">Madrid</option>
  ...
  <option value="12">Berlin</option>
  
</select>
-->
<!-- <%= select_tag(:description, options_for_select([['Lisbon', 1], ['Madrid', 2]])) %> -->
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description, class: "form-control" %><br>
  </div>
   <div class="field">
    <%= f.label :wodtime %><br>
    <%= f.text_field :wodtime, class: "form-control" %><br>
  </div>
 <!--
 <%= check_box_tag(:description) %>
  <%= label_tag(:description, "Snatch") %>
<%= check_box_tag(:description) %>
  <%= label_tag(:description, "Box Jumps") %>
-->
  <div class="actions">
    <%= f.submit "Submit Wod", class: "btn btn-danger" %>
  </div>
<% end %>
